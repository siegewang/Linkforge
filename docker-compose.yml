services:
  linkforge:
    build: .
    container_name: linkforge
    restart: unless-stopped
    ports:
      - "5006:5000"
    environment:
      - FLASK_DEBUG=0
    volumes:
      - ./data:/app/data
